 export enum AccountStatusEnum {
  ACTIVE = "active",
  BLOCKED = "blocked",
}

export enum VerificationStatusEnum {
  PENDING = "pending",
  VERIFIED = "verified",
  REJECTED = "rejected",
}

export enum PaymentStatusEnum {
  PENDING = "pending",
  PAID = "paid",
  FAILED = "failed",
  REFUNDED = "refunded",
}
